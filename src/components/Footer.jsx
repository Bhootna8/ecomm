import React, { useContext } from 'react'

const Footer = () => {

  return (
    <footer className={`dark:bg-white bg-black flex items-center w-full h-[300px] pt-10`}>

    </footer>
  )
}

export default Footer